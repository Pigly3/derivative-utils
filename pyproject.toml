[project]
name = "derivative-utils"
version = "0.1.0"
description = "[alpha] Tools for computing and working with derivatives of functions."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
