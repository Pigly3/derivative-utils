[project]
name = "derivative-utils"
version = "0.3.0"
description = "Tools for computing and working with derivatives of functions."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build.setuptools.packages.find]
where = ["."]